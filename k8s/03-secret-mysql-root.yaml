# =============================================================
# ADVERTENCIA DE SEGURIDAD
# Este archivo contiene credenciales PLACEHOLDER. NUNCA lo commitees con valores
# reales. Edita los valores antes de ejecutar kubectl apply y no los commitees.
# =============================================================
apiVersion: v1
kind: Secret
metadata:
  name: mysql-root-credentials
  namespace: sistema-despachos
type: Opaque
stringData:
  # IMPORTANTE: este valor debe coincidir EXACTAMENTE con DB_PASSWORD en
  # 01-secret-db-ventas.yaml y 02-secret-db-despachos.yaml. El contenedor MySQL
  # y ambos backends comparten el mismo usuario root y password.
  MYSQL_ROOT_PASSWORD: "etdevops2026"
